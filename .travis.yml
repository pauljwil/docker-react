language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t pauljwil/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pauljwil/docker-react npm run test
